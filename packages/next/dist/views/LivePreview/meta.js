import { generateMetadata as generateDocumentMetadata } from '../Edit/meta.js';
export const generateMetadata = async ({ collectionConfig, config, globalConfig, i18n, isEditing })=>generateDocumentMetadata({
        collectionConfig,
        config,
        globalConfig,
        i18n,
        isEditing
    });

//# sourceMappingURL=meta.js.map